# version: '3.8'

services:
  # ── nginx-rtmp: Live streaming server ──────────────────────────
  nginx-rtmp:
    image: tiangolo/nginx-rtmp
    container_name: nginx-rtmp
    ports:
      - "${HLS_PORT:-8080}:8080"    # HLS playback
      - "1935:1935"    # RTMP ingest
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
    tmpfs:
      - /tmp/hls:size=512m
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8080/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 5s
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    restart: unless-stopped
    networks:
      - video-network

  # ── Chunk generator: cron + manual triggers ─────────────────────
  chunk-generator:
    build:
      context: .
      dockerfile: Dockerfile.generator
    image: chunk-generator:latest
    container_name: chunk-generator
    volumes:
      - ./generate_chunk.sh:/generate_chunk.sh:ro
      - ${VIDEO_FOLDER:-./videos}:/videos:ro
      - ${CHUNK_FOLDER:-./chunks}:/chunks
      - ${STATS_DIR:-./stats}:/app/stats
      - trigger_volume:/app/trigger
    environment:
      - CHUNK_DURATION=${CHUNK_DURATION:-300}
      - CLIP_MIN=${CLIP_MIN:-5}
      - CLIP_MAX=${CLIP_MAX:-10}
      - CHUNKS_PER_RUN=${CHUNKS_PER_RUN:-4}
      - MAX_CHUNKS=${MAX_CHUNKS:-56}
      - HW_ACCEL=${HW_ACCEL:-none}
      - VIDEO_DIR=/videos
      - OUTPUT_DIR=/chunks
      - STATS_DIR=/app/stats
      - TRIGGER_DIR=/app/trigger
    shm_size: '512m'
    tmpfs:
      - /tmp:size=4g
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    restart: unless-stopped
    networks:
      - video-network

  # ── Streamer: shuffles through pre-made chunks ─────────────────
  random-video-streamer:
    build: .
    image: random-video-streamer:latest
    container_name: random-video-streamer
    user: root  # needed for Docker socket access (restart chunk-generator from UI)
    init: true
    ports:
      - "${PORT:-8081}:8080"
    volumes:
      - ${AUDIO_FOLDER:-./audio}:/audio:ro
      - ${CHUNK_FOLDER:-./chunks}:/chunks
      - ${STATS_DIR:-./stats}:/app/stats
      - trigger_volume:/app/trigger
      - /var/spool/cron/crontabs:/host-crontab:rw
      - ./.env:/app/.env
      - ./app.py:/app/app.py:ro
      - ./templates:/app/templates:ro
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      - CHUNK_FOLDER=/chunks
      - PORT=8080
      - EXTERNAL_PORT=${PORT:-8081}
      - RTMP_URL=rtmp://nginx-rtmp:1935/live/stream
      - AUDIO_FOLDER=/audio
      - HLS_PORT=${HLS_PORT:-8080}
      - STATS_DIR=/app/stats
      - TRIGGER_DIR=/app/trigger
      - HOST_CRONTAB_PATH=/host-crontab/root
      - PROJECT_ROOT=${PROJECT_ROOT:-}
    depends_on:
      nginx-rtmp:
        condition: service_healthy
    stop_grace_period: 30s
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:8080/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s
    shm_size: '256m'
    tmpfs:
      - /tmp:size=256m
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    deploy:
      resources:
        limits:
          cpus: "2.0"
          memory: 1G
    restart: unless-stopped
    networks:
      - video-network

volumes:
  trigger_volume:

networks:
  video-network:
    driver: bridge
